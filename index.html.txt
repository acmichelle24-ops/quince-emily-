<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quince Años de Emily - Bosque Encantado</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --morado: 
;
            --lila: 
;
            --dorado: 
;
            --tierra: 
;
            --bosque: 
;
            --verde-oscuro: 
;
            --blanco: 
;
            --crema: 
;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, var(--verde-oscuro) 0%, var(--bosque) 50%, var(--morado) 100%);
            color: var(--blanco);
            overflow-x: hidden;
            position: relative;
        }

        /* Partículas flotantes */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: var(--dorado);
            border-radius: 50%;
            animation: float 6s infinite ease-in-out;
            opacity: 0.6;
            box-shadow: 0 0 10px var(--dorado);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) translateX(0); opacity: 0.6; }
            50% { transform: translateY(-100px) translateX(50px); opacity: 1; }
        }

        /* Hero Section */
        .hero {
            position: relative;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            overflow: hidden;
            background: url('emily-1.jpg') center/cover no-repeat;
            background-attachment: fixed;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, rgba(44,62,47,0.7) 0%, rgba(139,90,142,0.6) 100%);
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            padding: 20px;
            animation: fadeInUp 1.5s ease;
        }

        .hero h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 5rem;
            font-weight: 700;
            color: var(--dorado);
            text-shadow: 0 4px 20px rgba(212,175,55,0.5);
            margin-bottom: 20px;
            letter-spacing: 3px;
        }

        .hero .tagline {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            font-style: italic;
            color: var(--lila);
            margin-bottom: 40px;
            text-shadow: 0 2px 10px rgba(0,0,0,0.5);
        }

        .countdown {
            display: flex;
            gap: 30px;
            justify-content: center;
            margin-top: 40px;
            flex-wrap: wrap;
        }

        .countdown-item {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            padding: 20px 30px;
            border-radius: 15px;
            border: 2px solid var(--dorado);
            box-shadow: 0 8px 32px rgba(0,0,0,0.3);
        }

        .countdown-item span {
            display: block;
            font-size: 3rem;
            font-weight: 700;
            color: var(--dorado);
        }

        .countdown-item p {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-top: 5px;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Secciones generales */
        section {
            padding: 100px 20px;
            position: relative;
            z-index: 2;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3.5rem;
            text-align: center;
            color: var(--dorado);
            margin-bottom: 50px;
            text-shadow: 0 2px 15px rgba(212,175,55,0.4);
        }

        /* Historia */
        .historia {
            background: rgba(139,90,142,0.2);
            backdrop-filter: blur(10px);
        }

        .historia-content {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
            font-size: 1.2rem;
            line-height: 2;
            color: var(--crema);
        }

        .historia-content p {
            margin-bottom: 20px;
        }

        /* Detalles del evento */
        .detalles {
            background: linear-gradient(135deg, rgba(74,93,79,0.3) 0%, rgba(44,62,47,0.5) 100%);
        }

        .detalles-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            margin-bottom: 60px;
        }

        .detalle-card {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(15px);
            padding: 40px;
            border-radius: 20px;
            border: 2px solid var(--lila);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .detalle-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 40px rgba(212,175,55,0.3);
        }

        .detalle-card h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            color: var(--dorado);
            margin-bottom: 15px;
        }

        .detalle-card p {
            font-size: 1.1rem;
            line-height: 1.8;
        }

        .mapa {
            width: 100%;
            height: 400px;
            border-radius: 20px;
            border: 3px solid var(--dorado);
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0,0,0,0.4);
        }

        /* Galería */
        .galeria {
            background: rgba(200,162,200,0.15);
        }

        .galeria-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .galeria-item {
            position: relative;
            overflow: hidden;
            border-radius: 15px;
            aspect-ratio: 1;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .galeria-item:hover {
            transform: scale(1.05);
        }

        .galeria-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .galeria-item:hover img {
            transform: scale(1.1);
        }

        /* Código de vestimenta */
        .vestimenta {
            background: linear-gradient(135deg, rgba(139,115,85,0.3) 0%, rgba(74,93,79,0.4) 100%);
        }

        .vestimenta-content {
            max-width: 900px;
            margin: 0 auto;
            text-align: center;
        }

        .vestimenta-content p {
            font-size: 1.3rem;
            margin-bottom: 30px;
            color: var(--crema);
        }

        .paleta-colores {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 20px;
            border: 2px solid var(--dorado);
            margin-bottom: 30px;
        }

        .paleta-colores img {
            max-width: 100%;
            border-radius: 10px;
            margin: 20px 0;
        }

        .prohibido {
            background: rgba(255,100,100,0.2);
            border: 2px solid 
;
            padding: 25px;
            border-radius: 15px;
            margin-top: 30px;
        }

        .prohibido h3 {
            color: 
;
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        /* RSVP */
        .rsvp {
            background: rgba(139,90,142,0.3);
            text-align: center;
        }

        .rsvp-btn {
            display: inline-block;
            background: linear-gradient(135deg, var(--dorado) 0%, 
 100%);
            color: var(--verde-oscuro);
            padding: 25px 60px;
            font-size: 1.5rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 50px;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(212,175,55,0.4);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .rsvp-btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 50px rgba(212,175,55,0.6);
        }

        /* Reproductor de música */
        .music-player {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 1000;
            background: rgba(139,90,142,0.9);
            backdrop-filter: blur(10px);
            padding: 15px 25px;
            border-radius: 50px;
            border: 2px solid var(--dorado);
            display: flex;
            align-items: center;
            gap: 15px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.4);
        }

        .music-btn {
            background: var(--dorado);
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.3s ease;
        }

        .music-btn:hover {
            transform: scale(1.1);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 3rem;
            }

            .hero .tagline {
                font-size: 1.2rem;
            }

            h2 {
                font-size: 2.5rem;
            }

            .countdown-item span {
                font-size: 2rem;
            }

            .countdown {
                gap: 15px;
            }

            .countdown-item {
                padding: 15px 20px;
            }
        }

        /* Scroll suave */
        html {
            scroll-behavior: smooth;
        }
    </style>
</head>
<body>
    <!-- Partículas flotantes -->
    <div class="particles" id="particles"></div>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Emily</h1>
            <p class="tagline">"Donde la magia del bosque se encuentra con los sueños de una princesa"</p>
            
            <div class="countdown" id="countdown">
                <div class="countdown-item">
                    <span id="days">00</span>
                    <p>Días</p>
                </div>
                <div class="countdown-item">
                    <span id="hours">00</span>
                    <p>Horas</p>
                </div>
                <div class="countdown-item">
                    <span id="minutes">00</span>
                    <p>Minutos</p>
                </div>
                <div class="countdown-item">
                    <span id="seconds">00</span>
                    <p>Segundos</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Historia -->
    <section class="historia">
        <div class="container">
            <h2>Una Historia Mágica</h2>
            <div class="historia-content">
                <p>En un rincón del bosque donde las luces danzan entre los árboles y las mariposas susurran secretos antiguos, una joven princesa está a punto de cumplir su sueño más esperado.</p>
                <p>Emily te invita a ser parte de este momento único, donde la magia de la naturaleza se fusiona con la elegancia de una celebración inolvidable.</p>
                <p>Acompáñanos en este viaje encantado, donde cada detalle ha sido pensado para crear recuerdos que brillarán por siempre.</p>
            </div>
        </div>
    </section>

    <!-- Detalles del Evento -->
    <section class="detalles">
        <div class="container">
            <h2>Detalles del Evento</h2>
            <div class="detalles-grid">
                <div class="detalle-card">
                    <h3>📅 Fecha</h3>
                    <p>26 de Julio<br>2025</p>
                </div>
                <div class="detalle-card">
                    <h3>🕔 Hora</h3>
                    <p>5:00 PM</p>
                </div>
                <div class="detalle-card">
                    <h3>📍 Lugar</h3>
                    <p>Restaurante Don Víctor<br>Carretera de Oro, Soyapango</p>
                </div>
            </div>

            <!-- Mapa de Google -->
            <iframe class="mapa" 
                src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3875.5!2d-89.15!3d13.7!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zMTPCsDQyJzAwLjAiTiA4OcKwMDknMDAuMCJX!5e0!3m2!1ses!2ssv!4v1234567890" 
                allowfullscreen="" 
                loading="lazy">
            </iframe>
        </div>
    </section>

    <!-- Galería -->
    <section class="galeria">
        <div class="container">
            <h2>Momentos Mágicos</h2>
            <div class="galeria-grid">
                <div class="galeria-item">
                    <img src="hero-bg.jpg" alt="Emily 1">
                </div>
                <div class="galeria-item">
                    <img src="Emi.jpg" alt="Emily 2">
                </div>
                <div class="galeria-item">
                    <img src="emily-1.jpg" alt="Emily 3">
                </div>
            </div>
        </div>
    </section>

    <!-- Código de Vestimenta -->
    <section class="vestimenta">
        <div class="container">
            <h2>Código de Vestimenta</h2>
            <div class="vestimenta-content">
                <p><strong>Semiformal</strong></p>
                <p>Te invitamos a vestir en armonía con nuestro bosque encantado</p>
                
                <div class="paleta-colores">
                    <h3 style="color: var(--dorado); margin-bottom: 20px;">Paleta de Colores Sugerida</h3>
                    <img src="paleta.jpg" alt="Paleta de colores">
                    <p style="margin-top: 20px;">Tonos tierra • Tonos del bosque • Verde • Café • Terracota</p>
                </div>

                <div class="prohibido">
                    <h3>⚠️ Colores Reservados</h3>
                    <p>Por favor, evita vestir de:<br><strong>Beige • Lila • Morado • Champagne</strong></p>
                </div>
            </div>
        </div>
    </section>

    <!-- RSVP -->
    <section class="rsvp">
        <div class="container">
            <h2>Confirma tu Asistencia</h2>
            <p style="font-size: 1.2rem; margin-bottom: 40px;">Tu presencia es el mejor regalo</p>
            <a href="https://wa.me/50377552766?text=Hola!%20Confirmo%20mi%20asistencia%20a%20los%20quince%20años%20de%20Emily" 
               class="rsvp-btn" 
               target="_blank">
                Confirmar por WhatsApp
            </a>
        </div>
    </section>

    <!-- Reproductor de música -->
    <div class="music-player">
        <button class="music-btn" id="musicToggle">▶️</button>
        <span>Rises the Moon - Liana Flores</span>
    </div>

    <audio id="bgMusic" loop>
        <source src="rises-the-moon.mp3" type="audio/mpeg">
    </audio>

    <script>
        // Cuenta regresiva
        const eventDate = new Date('2025-07-26T17:00:00').getTime();

        function updateCountdown() {
            const now = new Date().getTime();
            const distance = eventDate - now;

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById('days').textContent = days.toString().padStart(2, '0');
            document.getElementById('hours').textContent = hours.toString().padStart(2, '0');
            document.getElementById('minutes').textContent = minutes.toString().padStart(2, '0');
            document.getElementById('seconds').textContent = seconds.toString().padStart(2, '0');

            if (distance < 0) {
                document.getElementById('countdown').innerHTML = '<h3>¡El evento ha comenzado!</h3>';
            }
        }

        setInterval(updateCountdown, 1000);
        updateCountdown();

        // Partículas flotantes
        const particlesContainer = document.getElementById('particles');
        for (let i = 0; i < 50; i++) {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.left = Math.random() * 100 + '%';
            particle.style.top = Math.random() * 100 + '%';
            particle.style.animationDelay = Math.random() * 6 + 's';
            particle.style.animationDuration = (Math.random() * 4 + 4) + 's';
            particlesContainer.appendChild(particle);
        }

        // Reproductor de música
        const music = document.getElementById('bgMusic');
        const musicToggle = document.getElementById('musicToggle');
        let isPlaying = false;

        musicToggle.addEventListener('click', () => {
            if (isPlaying) {
                music.pause();
                musicToggle.textContent = '▶️';
            } else {
                music.play();
                musicToggle.textContent = '⏸️';
            }
            isPlaying = !isPlaying;
        });

        // Animaciones al scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        document.querySelectorAll('section').forEach(section => {
            section.style.opacity = '0';
            section.style.transform = 'translateY(50px)';
            section.style.transition = 'all 1s ease';
            observer.observe(section);
        });
    </script>
</body>
</html>